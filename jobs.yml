- name: Create Jenkins Jobs
  hosts: localhost
  connection: local
  vars:
    jobs:
      - roboshop-terraform
      - cart
      - catalogue
      - user
      - payment
      - shipping
      - frontend
      - dispatch
  roles:
    - jenkins-jobs